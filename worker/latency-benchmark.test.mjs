import test from 'node:test';import assert from 'node:assert/strict';const delay=ms=>new Promise(resolve=>setTimeout(resolve,ms));
test('mocked fast path removes config and login round trips',async()=>{let legacyCalls=0,fastCalls=0;const upstream=async mode=>{await delay(6);if(mode==='legacy')legacyCalls++;else fastCalls++};const legacyStart=performance.now();await upstream('legacy');await upstream('legacy');await upstream('legacy');const legacyMs=performance.now()-legacyStart;const fastStart=performance.now();await upstream('fast');const fastMs=performance.now()-fastStart;assert.equal(legacyCalls,3);assert.equal(fastCalls,1);assert.ok(fastMs<legacyMs/2,`fast=${fastMs} legacy=${legacyMs}`)});
