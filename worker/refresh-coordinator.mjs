export async function coordinateRefresh({version,tryAcquire,readCurrent,refresh,save,release,wait,attempts=4}){if(await tryAcquire(version)){try{const fresh=await refresh();if(!fresh){await release(version);return null}await save(fresh,version);return fresh}catch(error){await release(version);throw error}}for(let attempt=0;attempt<attempts;attempt++){await wait();const current=await readCurrent();if(current&&current.version>version)return current.session}return null}
